services:
  nginx:
    image: nginx:latest
    container_name: nginx-secure
    ports:
      - "443:443"
    volumes:
      - ./default.conf:/etc/nginx/conf.d/default.conf
      - ./ssl:/etc/nginx/ssl